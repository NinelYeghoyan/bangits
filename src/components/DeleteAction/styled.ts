import styled from 'styled-components';

const StyledDeleteAction = styled.div`
    width: 26px;
    height: 26px;

    color: ${(p) => p.theme.palette.orange};
`;

export default StyledDeleteAction;
